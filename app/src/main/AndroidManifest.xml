<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidLabTest501"
        tools:targetApi="31">
        <activity
            android:name=".miniProject.test0703.msy0402_mini.Msy0402LoginActivity"
            android:exported="true" />
        <activity
            android:name=".miniProject.test0703.msy0402_mini.Msy0402SplashActivity"
            android:exported="true" />
        <activity
            android:name=".miniProject.test0703.msy0402_mini.Msy0402JoinActivity"
            android:exported="true" />
        <activity
            android:name=".miniProject.test0703.msy0402_mini.Msy0402MainActivity"
            android:exported="true" />

        <!-- TestActivity 우클릭 하고 따로 실행. 주의사항, -->
        <!-- exported="true" 변경, 각각 실행됨. -->
        <activity
            android:name=".TestActivity"
            android:exported="true" /> <!-- 앱을 만들면, 기본 MainActivity 시작화면으로 지정. -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>

